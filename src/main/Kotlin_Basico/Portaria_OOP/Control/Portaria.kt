package Portaria_OOP

class Portaria{

    fun controle(){
        val idade = Console.readInt("Digite sua idade: ")
        if (idade < 18){
            println("Negado, menores de idade não são permitidos.")
            return
        }

        val tipoConvite = Console.readString("Digite o tipo do seu convite: ")
        if (tipoConvite )
    }
}